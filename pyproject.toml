[project]
name = "trash-can"
version = "0.1.0"
description = "Trash-can classifier: phone photo in, Jev bin decision out."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "typesafe-sdk>=0.5.7",
  "httpx>=0.27",
  "python-multipart>=0.0.9",
  "python-dotenv>=1.0",
  "pillow>=10.0",
]

[tool.uv]
package = false
